/**
 * Configuración de entorno para producción.
 * production: Indica si el entorno es productivo.
 * apiUrl: URL base de la API utilizada en producción.
 */
export const environment = {
  /**
   * Indica si el entorno es productivo.
   */
  production: true,
  /**
   * URL base de la API utilizada en producción.
   */
  apiUrl: 'http://localhost:3000/heroes',
};
